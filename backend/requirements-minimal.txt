# Minimal dependencies for testing without native extensions
# Using pydantic v1 to avoid Rust dependency

# FastAPI & Web (older versions that work with pydantic v1)
fastapi==0.100.1
uvicorn==0.23.2
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic>=1.10.0,<2.0.0

# Utilities (pure Python)
python-dotenv==1.0.0
structlog==24.1.0
httpx==0.26.0
beautifulsoup4==4.12.3

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
